// output -> f
// let i = 0;
// while(i < str.length){
    //     let substring = str.substring(i+1,str.length-1);
    //     if(!substring.includes(str[i])){
        //         console.log("char Found",str[i]);
        //         break;
        //     }
        //     i++;
        // }
        // console.log("char Not Found");

let str = 'greekforgreeks';
let findStr = "eek";
let replaceStr = 'ok';
function getReplacedString(str,findStr, replaceStr){
    let i = 0;
    let temp = '';
    while(i < str.length){
        if(temp === findStr){
            
        }
    }
}
